<div class="container mt-4">
<h1>Add a movie to this list</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>
<%= link_to "<< Back to all lists", lists_path, class: "btn btn-flat mt-3" %>
</div>
